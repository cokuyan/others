<h3>Notes</h3>

<ul>
  <% notes.each do |note| %>
    <li>
      <%= note.body %><br>
      <%= note.user.email %>
      <% if current_user.admin || note.user == current_user %>
        <%= button_to "Remove", note_url(note), method: :delete %>
      <% end %>
    </li>
  <% end %>
</ul>
