<h1><%= @user.user_name %></h1>

<p>Hello, dear user! You are logged in from the following locations:</p>

<ul>
  <% @user.sessions.each do |session| %>
    <li>
      <%= session.user_agent %>
      <!-- <%= button_to "log out", session_url, method: :delete %> -->
    </li>
  <% end %>
</ul>
